# Justificación: por qué async/await mejora la legibilidad

> Reemplaza este texto con tu explicación (unas 100 palabras como mínimo, con tus propias palabras).

> **Nota de divulgación:** Esta plantilla fue elaborada con asistencia de Claude (Anthropic) y revisada por Jesús Armando Tapia Gallegos. La explicación que escribas es de tu autoría.

La consigna del manual pide una *justificación escrita de por qué async/await mejora la legibilidad*. Responde:

1. Compara las tres versiones del flujo de usuarios de `actividad3.js` (`flujoUsuariosCallbacks`, `flujoUsuariosPromesas` y `flujoUsuariosAsync`). ¿Por qué la de async/await se lee mejor y es más fácil de mantener? Apóyate en cómo se vería agregar un cuarto usuario y en el manejo de errores.
2. En `listarUsuarios`, ¿cómo distingues un error de red de uno HTTP y de uno de JSON? Si el servidor responde 404, ¿`fetch()` rechaza la promesa por sí solo? ¿Por qué?
3. ¿Qué cambió entre callback, Promise y async/await: la operación o la forma de escribir el flujo?

TODO: escribe aquí tu explicación.
