// Responsabilidad: TODO — describe en una frase qué hace este archivo.
// Versión 2 — Promises con fetch, .then/.catch/.finally (sin async/await).
// Contrato en el README de la actividad. Explica en comentarios QUÉ hace cada paso y POR QUÉ.

import { ErrorRed, ErrorHTTP } from './errores.js';

export function obtenerUsuario(baseUrl, id) {
  // TODO
}

export function obtenerUsuarioConPosts(baseUrl, id) {
  // TODO
}
